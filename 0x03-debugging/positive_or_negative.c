#include "main.h"
/**
 * positive_or_negative - function that perform the opration
 * @i: checking variable
 * Return: nothing becouse void
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is postive", i);
	else
		printf("%d is negative", i);
}


