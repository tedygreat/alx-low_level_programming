#include "main.h"

/**
 * mul - function that perform multiplication
 * @a: first variable
 * @b: second variable
 * Return: lways return result;
 */

int mul(int a, int b)
{
		int result;
			result = a * b;
	return (result);
}
