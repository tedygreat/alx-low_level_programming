#include "main.h"
#include <unistd.h>
/**
 * print_alphabet - print the alphabet of lower ca
 */
void print_alphabet(void)
{
		char l;
  int count;
  for (coun = 0; count < 10; count++)
  {
	for (l = 'a'; l <= 'z'; l++)
		_putchar(l);
	_putchar('\n');
  }
}
